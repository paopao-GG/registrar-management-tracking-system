Add Status "Pending": After saving a new request, status becomes "Pending" instead of processing.

Add Action "Start Processing": Instead of automatic "Processing" status right after saving the request, add a trigger to change the status to Processing.

Add Status "Ready for Release": After the document is signed/reviewed, change the status to this instead of "Signed"

Remove "Release" action from Admin.