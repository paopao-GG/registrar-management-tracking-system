Updates to make in general:
1. Change the Programs seen in the Program column to its abbreviated forms so the columns aren't so wide.
2. Add a bulk selection and bulk action in the left side of the date column and include bulk signing in the actions because mostly, one representative can claim documents for the whole block.
3. Hide the pages in a menu bar to widen the main interface.
4. Remove Reviewed/Signed By column only in the dashboards but it should still be seen in the reports.
5. Move the Actions column beside the Status column.
6. Reduce the size of the Dates.
7. Transactions seen should be per day so swap the placement of the date and status filters and do not show transactions that aren't created in the current day.
8. If a scroll bar should exist, place it on top of the columns instead of bottom.

Updates to make in the Admin Dashboard:
Students page: 
1. Change the programs to the abbreviated forms to make more space for the emails.
2. All students from the imported list must be shown as Enrolled Students rather than Recent Students.
3. Add view filter by program.
4. Show total number of students somewhere above.
5. Change Email to BU Email.
6. Add Contact Number Column

Reports page: 
1. Add another download option for the BUP Logbook.
2. The first/existing download option should be renamed to "ARTA-Logbook export" and the second download option being "BUP-Logbook export".
3. Both should have a header: COLLEGE/CAMPUS: BICOL UNIVERSITY POLANGUI and a background of orange before the main columns.
4. Columns in ARTA-Logbook are: External Client Name, Requested Documents/Services, Contact Number, University Email Address, and Date of Transaction (when it was completed/released).
5. Columns in BUP-Logbook are: Date, Name, Sex (M or F), Course & Year Level, Requested Documents/Services [This has sub-columns: COR, COG, GMC, AUTH, OTR, OTHERS. The total number of requests per student and document should be reflected in these columns.] Received/Prepared By, Reviewed/Signed By, Duration of Process, Released To/Claimed By [These last 3 columns should have the names, date, and time].
6. If possible, add a Signature column in the end of the BUP-Logbook and add the photo of the captured signature in a sense that it's like an e-sign with the background removed.
7. In the end of the list in BUP-Logbook is the Total number in each Document Request.

Audit Log Page:
1. Remove Transaction ID.
2. Add total logs, filter by current day.

Updates to make in the Staff Dashboard:
1. Remove Add Student from the dropdown in Student Name.
2. Add Alumni button to save a request from an alumni with the details asked the same behavior as the previous Add Student option.
3. Correct the CMC, it should be GMC.

Updates to make in the Sign Dashboard:
1. Below the signature space just above the Done button, add a tick box to confirm that they agree about their signature being captured, something like that about the Data Privacy Act.

For security purposes:
Currently, sign tab is accessible in any device when you have or you know the URL. Rather, I think it should be accessible to one device only, like it can't be accessed in another when already open. Also, it should only be active when a staff is active.